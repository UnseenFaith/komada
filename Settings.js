class Settings {
  constructor(keys, values, structure) {
    // do something with keys and values here
    // structures hieracrchy - Client (Global) > Guild > Channel > User
  }

  get() { return a proxy }
  set() {}
  add() {}
  delete() {}
  save() {} // maybe?

}

module.exports = Settings;
