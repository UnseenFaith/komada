const Cache = require("./Cache");

class Settings {
    constructor() {
        this.gateway;
        this.cache = new Cache(client, type);
        this.schema;
        this.resolver;
    }
}

module.exports = Settings;
